h2 List Transaction
ul
  
  
  table
    tr
      th id
      th user
      th book
      th Complete
  table
    if transactions.length > 0
      each item in transactions
        if item.isComplete === false
          tr
            td=item.id
            td=item.userId
            td=item.bookId
            td 
              a(href=`/transactions/${item.id}/complete`) Complete
    else
      tr
        td ...
        td ...
        td ...

a(href='/transactions/create') Create new transaction